b :: int = 7; c :: int = 8;

main :: fn () -> int
{
    D := 9u;
    D ? ppl_console_print( "Hello: %d\n", b + c + D) :
        ppl_console_print( "Hellohi: %d\n", c);

    for 0 ..= 7 do
    {
        ppl_console_print("it's loop time dude!\n");

        if d then
        {
            ppl_console_print("d is nonzero\n");
        }
        else
        {
            ppl_console_print("d is zero\n");
        }

        while i := 0; true; b + c
            do 
        ppl_console_print( "while loop still spinning :)\n" );
    }
}

d :: 0


