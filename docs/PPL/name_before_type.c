
// we put the name before the type for readability.
// this way, you get to read what the meaning of the thing is first.
myInt : int = 10;

doComplicatedStuff : (a : float, b : float) -> float {
    return a + b;
}